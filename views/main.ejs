<!DOCTYPE html>
<html lang="ja">
    <head>
        <title>TODOリスト</title>
        <%-include('./header')%>
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
    <article>
    <h1>TODOリスト</h1>
    <section>
    <h3 class="must">しないといけないこと</h3>
    <% for(let item of m){ %>
        <p><%= item %></p>
    <% } %>
    </section>

    <section>
    <h3 class="want">やりたいこと</h3>
    <% for(let item of w){ %>
        <p><%= item %></p>
    <% } %>
    </section>

    <section>
    <h3 class="eventually">そのうちすること</h3>
    <% for(let item of e){ %>
        <p><%= item %></p>
    <% } %>
    </section>
    </article>
    </body>
</html>